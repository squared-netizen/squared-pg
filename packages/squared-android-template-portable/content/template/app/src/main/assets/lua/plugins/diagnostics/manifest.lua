--- Diagnostics plug-in manifest.
-- @script diagnostics.manifest

return {
    id = "diagnostics",
    version = "1.0.0",
    api_version = 1,
    entry = "main",
    capabilities = {
        "log"
    }
}
