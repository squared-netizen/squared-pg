// {{project_name}} — entry point.
//
// This file is YOURS (ownership class: seeded). The generator wrote it once
// and will not touch it again.

#include "app.hpp"

int main(int argc, char** argv) {
    {{project_name}}::App app;
    return app.run(argc, argv);
}
