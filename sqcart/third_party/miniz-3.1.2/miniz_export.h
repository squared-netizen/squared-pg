/* miniz_export.h — build-support stub.
 *
 * miniz normally generates this file with generate_export_header(). sqcart
 * builds miniz as a static library with default visibility, so MINIZ_EXPORT
 * expands to nothing. This stub is a local build-support addition to the
 * vendored tree, not part of upstream miniz.
 */
#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H

#define MINIZ_EXPORT

#define MINIZ_STATIC_DEFINE

#endif  /* MINIZ_EXPORT_H */
