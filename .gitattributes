# Test fixtures are digested byte-for-byte. Never normalize them.
sqcart/tests/** -text

# Cartridges are ZIP containers.
*.sq binary

# Vendored sources: leave exactly as shipped upstream.
sqcart/third_party/** -text
third-party/** -text

# Everything else: normalize to LF in the repo.
* text=auto eol=lf
